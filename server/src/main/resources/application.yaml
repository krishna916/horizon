spring:
  application:
    name: horizon
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5433/horizon?sslmode=disable
    username: horizon
    password: horizon
server:
  port: 8081
